name: gitleaks
on:
  pull_request:
    # branches:
    #     - 'main'
    #     - 'develop'
  push:
    # branches:
    #     - 'main'
    #     - 'develop'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13' 
      
      - name: Install GitLeaks
        run: |
          set +e
          docker pull ghcr.io/gitleaks/gitleaks:latest
          docker run -v ${{ github.workspace }}:/repoFiles ghcr.io/gitleaks/gitleaks:latest git --redact -v --exit-code=2 -f=sarif -r=/repoFiles/results.sarif -l=debug /repoFiles
          exitcode="$?"
          echo "exitcode=$exitcode" >> $GITHUB_OUTPUT 
          python3 .github/summary_scripts/githubSummary.py -exit_code $exitcode -repo_url ${{ github.server_url }}/${{ github.repository }}
          ls -lrt
          cat ${{ github.workspace }}/extracted_results.md >$GITHUB_STEP_SUMMARY
          exit "$exitcode"
        #continue-on-error: true


